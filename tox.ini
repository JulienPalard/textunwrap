[tox]
envlist = py36, py37, py38, py39, flake8
isolated_build = True

[testenv:flake8]
basepython=python
deps=flake8
commands=flake8 textunwrap

[testenv]
deps = -r requirements-dev.txt
commands =
    pip install -U pip
    pytest
